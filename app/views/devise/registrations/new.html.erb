<div id="podcast_show">
  <div id="show_banner">
    <div class="wrapper_skinny">
      <h1>Sign up, and Update your Profile!</h1>
    </div>
  </div>
<div id = "form">
  <div class="wrapper_skinny">


        <h3>Yo, Thanks for signing up. <br> I built it to make things easier to meet new friends in different city.</h3>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <div class="field">
          <%= f.label :email %> (It can be anything, but .edu or .ac is preferred 'cuz it looks cool, haha)
            <br />
            <%= f.email_field :email, autofocus: true %>
          </div>
          <div class="field">
            <%= f.label :name %> - What your mom calls u :)<br />
            <%= f.text_field :name, autofocus: true %>
          </div>
          <div class="field">
            <%= f.label :password %>
            <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
            <% end %><br />
            <%= f.password_field :password, autocomplete: "off" %>
          </div>

          <div class="field">
            <%= f.label :password_confirmation %> - yah, it bothers me too. <br />
            <%= f.password_field :password_confirmation, autocomplete: "off" %>
          </div>

          <div class="field">
            <%= f.label :city %> - where you are now staying (I mean like where u live)<br />
            <%= f.text_field :city %>
          </div>
          <div class="field">
            <%= f.label :photo %>. No need to upload now, but I kinda want to see your face too. You can see mine.<br />
            <%= f.file_field :photo  %>
          </div>
<br>

            <h3>By Signing up, your email and info will be shared in ExploreCity&School Page.</h3>


          <div class="actions">
            <%= f.submit "Sign up", class: "button button_highlight button_block"%>
          </div>
          <br /><br />
        <% end %>

        <%= render "devise/shared/links" %>

  </div>
</div>
