<%= simple_form_for([@student, @student.studentcomments.build]) do |f| %>
  <%= f.input :comment %>
  <div class="field">
    <%= f.label "Upload Photo" %><br>
    <%= f.file_field :photo %>
  </div>
  <%= f.submit "Create Comment", class: "button button_highlight" %>
<% end %>
